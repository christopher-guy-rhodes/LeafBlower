const STOP = 'stop';
const WALK = 'walk';
const RUN = 'run';
const SCROLLING_ACTIONS = [WALK, RUN];

const RIGHT = 'right';
const LEFT = 'left';

const BACKGROUND_WIDTH_PX = 1334;
const BACKGROUND_HEIGHT_PX = 750;

const PPS = 'pps';
const FPS = 'fps';

export {BACKGROUND_WIDTH_PX, BACKGROUND_HEIGHT_PX, FPS, LEFT, PPS, RIGHT, RUN, SCROLLING_ACTIONS, STOP, WALK}
